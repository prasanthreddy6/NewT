<div id="myCarousel" class="carousel slide" data-ride="carousel">
  <!-- Indicators -->
  <ol class="carousel-indicators">
    <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
    <li data-target="#myCarousel" data-slide-to="1"></li>
    <li data-target="#myCarousel" data-slide-to="2"></li>
  </ol>

  <!-- Wrapper for slides -->
  <div class="carousel-inner" role="listbox">
    <div class="item active">
      <img src="/chevy.jpg" style="width:100%; height:300px;">

    </div>

    <div class="item">
      <img src="/image1.png" style="width:100%; height:300px;">
    </div>

    <div class="item">
      <img src="/image2.png" style="width:100%; height:300px;">
    </div>
  </div>

  <!-- Left and right controls -->
  <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
    <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
    <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>


<div style="padding-top:10px">
    <h1>Recent Championships</h1>
    <% @championship.all.order("created_at DESC").limit(5).each do |d| %>
      <%= link_to ( image_tag d.pic.url(:square) ), {:controller => 'championships' , :action => 'show', :id => d.id} %>
    <%end%>
</div>

<div style="padding-top:10px">
    <h1>Recent Gold Medalist</h1>
    <% @champ.all.order("created_at DESC").limit(5).each do |d| %>
      <%= link_to ( image_tag d.user.profile.avatar.url(:square) ), {:controller => 'championships' , :action => 'show', :id => d.id} %>
    <%end%>
   
<%= link_to "Change your password", edit_user_registration_path %> &nbsp &nbsp<%= link_to "view profile", profiles_profileview_path %>

</div>